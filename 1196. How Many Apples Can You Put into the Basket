class Solution {
public:
    int maxNumberOfApples(vector<int>& v) {
        sort(v.begin(),v.end());
        int s=0,i;
        for(i=0;i<v.size();i++)
        {
            s=s+v[i];
            if(s>5000)
                break;
                
        }
      return i;
    }
};
